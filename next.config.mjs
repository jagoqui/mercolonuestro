/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "dummyimage.com",
      "images.unsplash.com",
      "via.placeholder.com",
      "storage.googleapis.com",
    ], // Agrega aquí el dominio
  },
};

export default nextConfig;
